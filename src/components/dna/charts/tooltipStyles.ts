export const dnaChartTooltipProps = {
  contentStyle: {
    backgroundColor: "var(--dna-surface-3)",
    border: "1px solid var(--dna-surface-border-strong)",
    borderRadius: "12px",
    boxShadow: "var(--dna-shadow-tight)",
  },
  labelStyle: {
    color: "inherit",
    fontWeight: 600,
  },
} as const;
